# تغییرات پروژه CoCAI

فرمت این فایل بر اساس [Keep a Changelog](https://keepachangelog.com/fa-IR/1.1.0/) است.

## [1.4.0] - 2026-09-03

### افزوده‌شده
- **آرشیو واقعی نقشه‌ها**: ۱۳٬۳۷۳ نقشهٔ Clasher.us با لینک واقعی `OpenLayout` (از دامپ `cocai.sql` ژانویه ۲۰۲۵) به پروداکشن و محیط محلی بازگردانده شد؛ ۱۶۱ topic با سطح تاون‌هال/بیلدرهال، ۷۱ هزار ارتباط map_topic، guides و units.
- **اسپرایت‌های واقعی بازی**: manifest `database/data/coc/sprites.json`، دستور `php artisan coc:sprites` (فایل‌های محلی + تکمیل از ویکی Fandom با API)، ۱۰۵ اسپرایت در `public/images/coc/buildings/{home,builder,walls,ground}` (۳٫۸ مگابایت، ≤۲۵۶px)؛ `BuildingCatalog::get()` کلید `sprite` و `spriteFor(type, level)` (تاون‌هال/بیلدرهال به تفکیک سطح) می‌دهد.
- **رندر ایزومتریک شبیه بازی** (`Components/Dashboard/Iso/*`): SVG با یک transform، لوزی چمن، دیوارهای خودکار متصل، اسپرایت‌ها لنگرشده به کف footprint، z-order نقاش، tooltip/انتخاب، نشان «؟» برای ساختمان‌های نامطمئن، pan/zoom/fit، خروجی PNG.
- **ویرایشگر چیدمان برای مالک** (`Components/Dashboard/Editor/*`): جابه‌جایی با درگ و snap، افزودن از پالت، حذف، تغییر نوع/سطح، رسم/پاک‌کردن دیوار، undo/redo، پنل «نامطمئن‌ها» با تأیید/جای‌گذاری/حذف، D-pad، ذخیره با `PUT /api/base-clones/{slug}/layout` (اعتبارسنجی سخت‌گیرانهٔ سرور، ۴۰۹ برای نسخهٔ قدیمی)، `GET /api/base-clones/catalog`, `GET /api/base-clones/{slug}`.
- **دقت**: قرارداد Vision نسخهٔ ۲ با bbox (۰–۱۰۰۰)، `GeometrySolver` (مقیاس از boxes/گوشه‌ها/لوزی، محورهای ۲:۱ با گیت شیب دیوار، قفل شبکه)، `BuildingCaps` (سقف تعداد هر ساختمان بر اساس تاون‌هال ۹–۱۷)، `LayoutStats`، confidence/uncertain/flags/alternatives برای هر ساختمان، `layout.version=2`.
- **تطبیق آرشیو**: `LayoutSignature` (تطبیق نوع/موقعیت ±۲ خانه + IoU دیوار) کنار dHash؛ `Map::hasValidCopyLink()` فقط لینک‌های با فرمت واقعی را عبور می‌دهد؛ دستور `maps:signature --limit --th`؛ `maps:hash` تصویر کامل (`/images/full/`) را هش می‌کند (بندانگشتی فاصلهٔ ۱۲ می‌دهد، تصویر کامل ۰).
- تست‌ها: GeometrySolverTest، BuildingCapsTest، BaseCloneLayoutEditTest، LayoutSignatureMatchTest و به‌روزرسانی تست‌های قبلی (۱۶۵ تست).

### تغییریافته
- لینک‌های placeholder در `ClashMapSeeder` حذف شد (null)؛ `FetchClasher` روی https.
- یادداشت: تست‌ها را با `DB_CONNECTION=sqlite DB_DATABASE=:memory: CACHE_STORE=array` اجرا کنید تا دیتابیس توسعه پاک نشود.

## [1.3.0] - 2026-09-02

### افزوده‌شده
- **موتور چندبازی «بازسازی از روی عکس»** با معماری آداپتور (`app/Services/BaseClone/Games/`): هر بازی سوپرسل یک موتور مستقل دارد و در `GameRegistry` ثبت می‌شود.
- **کلش رویال — دک**: عکس دک (داخل بازی، صفحهٔ نتیجه یا سایت) → تشخیص ۸ کارت با Vision → تطبیق با کاتالوگ رسمی کارت‌ها (`database/data/cr/cards.json`) → **لینک رسمی کپی دک** که مستقیم در بازی باز می‌شود؛ پشتیبانی از Evolution و تاور تروپ (لینک `copyDeck` با `slots` و `tt`)، میانگین اکسیر و فهرست کارت‌های تشخیص‌داده‌نشده.
- **کلش آف کلنز — بیلدر بیس**: کاتالوگ جداگانهٔ ساختمان‌های بیلدر بیس (`BuilderBaseCatalog`) و پرامپت مخصوص دهکده.
- **دستور `php artisan cr:cards`** برای به‌روزرسانی شناسهٔ کارت‌ها از API رسمی (`CLASH_ROYALE_API_TOKEN`) یا دادهٔ عمومی RoyaleAPI؛ کارت‌های جدیدتر تا اجرای این دستور «تأییدنشده» علامت می‌خورند.
- **پنل جدید «کلونر AI»** به‌عنوان تب مستقل در نوار پایین داشبورد: انتخاب بازی (کلش آف کلنز دهکدهٔ اصلی/بیلدر بیس، کلش رویال؛ براول استارز، بوم بیچ و اسکواد باسترز به‌صورت «به‌زودی»)، آپلود با درگ‌ودراپ و Ctrl+V، فشرده‌سازی تصویر سمت کاربر، نمایش مراحل (آپلود → تحلیل → بازسازی → لینک)، اورلی اسکن هنگام تحلیل، کارت لینک کپی با دکمه‌های بزرگ، تاریخچهٔ بازسازی‌ها.
- **مؤلفهٔ `DeckCardList`** برای نمایش دک ۸ کارتی با رنگ کمیابی، اکسیر، Evolution و سطح.
- **دیپ‌لینک تب داشبورد**: `/dashboard?tab=cloner` (یا `#cloner`).
- `GET /api/base-clones/games` — فهرست موتورهای فعال و به‌زودی برای UI؛ پارامتر `game` در `POST /api/base-clones`.
- ستون‌های `game` و `copy_link` در جدول `base_clones`.
- تست‌ها: `CardCatalogTest`، `BaseCloneGamesTest`.

### تغییریافته
- **پایداری Vision**: alias اختصاصی تصویر (`NABU_VISION_MODEL`، پیش‌فرض `nabu-vision`) با fallback خودکار به `NABU_MODEL`؛ خطاهای gateway/توکن/مدل/timeout با پیام دقیق فارسی و کد `reason` (و وضعیت 503 به‌جای 422) برمی‌گردند؛ پاسخ خالی خطا محسوب می‌شود؛ content آرایه‌ای هم خوانده می‌شود؛ JSON قطع‌شده تا جای ممکن بازیابی می‌شود (`salvage`)؛ سقف زمان اجرای PHP برای فراخوانی Vision بالا می‌رود (سرور داخلی PHP در ۳۰ ثانیه می‌مرد).
- پرامپت‌های Vision از نمونه‌های عددی به placeholder تغییر کردند تا مدل مقادیر نمونه را کپی نکند.
- **اسکیمای فشردهٔ چیدمان** (`{"th","p","c","thb","b":[[type,x,y,level]],"w":[[x1,y1,x2,y2]]}`) تعداد توکن خروجی را نصف کرد (بیس TH15 واقعی: ۹۷ ثانیه → ۶۴ ثانیه، ۱۱۵ ساختمان + ۱۰۸ دیوار). provider پشت `nabu-vision` مقدار `max_tokens` را کل بودجه (ورودی + ~۶٬۵۰۰ توکن تصویر + خروجی) حساب می‌کند؛ سقف چیدمان ۲۴۰۰۰ و دک ۱۲۰۰۰ شد.
- `NABU_VISION_MODEL` می‌تواند فهرست با کاما باشد (پیش‌فرض `nabu-vision,nabu-ocr`)، `NABU_VISION_TIMEOUT` پیش‌فرض ۱۸۰ ثانیه، بدون تکرار در timeout؛ timeout درخواست فرانت ۲۴۰ ثانیه.
- صفحهٔ عمومی `/base/{slug}` بر اساس نوع نتیجه (چیدمان یا دک) رندر می‌شود و ظاهر آن با پنل جدید هماهنگ شد.
- کارت ورودی سریع به کلونر در تب «نقشه‌ها» جایگزین پنل قبلی شد.

### اصلاح‌شده
- **قطع کامل چت و پرسش‌وپاسخ هوش مصنوعی در محیط عملیاتی** («خطا در برقراری ارتباط با مرکز فرماندهی هوش مصنوعی»): مدل `openrouter2/*` روی گیت‌وی با 502 (`all targets failed / status 402: requires more credits`) شکست می‌خورد و هیچ جایگزینی امتحان نمی‌شد. کلاینت واحد `App\Services\AI\NabuGateClient` اضافه شد که زنجیرهٔ مدل‌ها را به ترتیب `NABU_MODEL` و سپس `NABU_FALLBACK_MODELS` (پیش‌فرض `nabu-smart,nabu-fast`) امتحان می‌کند: خطای اتصال → مدل بعدی، 5xx → یک تکرار سپس مدل بعدی، 4xx → مدل بعدی، پاسخ خالی → مدل بعدی. `ChatbotService` و `AiAgentService` به این کلاینت منتقل شدند (امضای عمومی متدها تغییری نکرد)؛ مهلت درخواست چت از ۱۵ به ۴۵ ثانیه رسید (`NABU_CHAT_TIMEOUT`)؛ محتوای آرایه‌ای و `choices[0].text` هم خوانده می‌شود؛ `lastError()`/`lastErrorMessage()` دلیل شکست (connection/timeout/auth/model/server/empty) را با پیام فارسی برمی‌گرداند. تست: `NabuGateClientTest`.

## [1.2.0] - 2026-09-02

### افزوده‌شده
- **بازسازی بیس از روی عکس (AI Base Cloner)**: در تب «نقشه‌ها» عکس هر بیس کلش را آپلود کنید؛ مدل Vision همهٔ ساختمان‌ها، قطعات دیوار، گوشه‌های لوزی نقشه و مقیاس تاون‌هال را استخراج می‌کند و `LayoutGridMapper` آن‌ها را به‌صورت قطعی روی شبکهٔ ۴۴×۴۴ می‌نشاند (رفع هم‌پوشانی، برش دیوارها، آمار دسته‌ها).
- **لینک اشتراک‌گذاری عمومی** `GET /base/{slug}`: صفحهٔ بدون نیاز به ورود با نمای بازی/شبکه، تصویر اصلی، فهرست و مختصات دقیق ساختمان‌ها و دکمهٔ کپی لینک.
- **تطبیق با آرشیو نقشه‌ها**: هش ادراکی (dHash) تصویر آپلودی با هش نقشه‌های Clasher.us مقایسه می‌شود؛ اگر همان بیس در آرشیو باشد، لینک اصلی «کپی در بازی» (OpenLayout) برگردانده می‌شود.
- **دستور `php artisan maps:hash`** برای محاسبهٔ هش تصویر نقشه‌های آرشیو (گزینه‌های `--force` و `--limit`).
- **API**:
  - `POST /api/base-clones` — آپلود تصویر (تا ۸ مگابایت) و بازسازی چیدمان (throttle ۱۰ در دقیقه)
  - `GET /api/base-clones` — فهرست بیس‌های بازسازی‌شدهٔ کاربر
  - `DELETE /api/base-clones/{slug}` — حذف
- **مدل `BaseClone`** و مهاجرت‌های `base_clones` و ستون `maps.image_hash`.
- **تست‌ها**: `LayoutGridMapperTest`، `ImageHasherTest` و `BaseCloneControllerTest`.

### تغییریافته
- `BaseVisionAnalyzer` پرامپت، دما و `max_tokens` را در متدهای قابل بازنویسی گذاشت تا `LayoutVisionExtractor` بتواند از همان مسیر فراخوانی استفاده کند.

### یادداشت فنی
- لینک `OpenLayout` بازی یک ارجاع ۲۴ بایتی به چیدمان ذخیره‌شده در سرور Supercell است و از روی تصویر قابل تولید نیست؛ به همین دلیل لینک داخل بازی فقط از طریق تطبیق با آرشیو ارائه می‌شود.

## [1.1.1] - 2026-08-23

### اصلاح‌شده
- **بازیابی رمز عبور**: ایمیل بازیابی رمز عبور قبلاً ارسال نمی‌شد زیرا از نوتیفیکیشن پیش‌فرض Laravel استفاده می‌کرد و در محیط توسعه `MAIL_MAILER=log` باعث می‌شد فقط در فایل لاگ نوشته شود.
- نوتیفیکیشن سفارشی `CustomResetPassword` جایگزین شد تا در تمام محیط‌ها (با تنظیم صحیح SMTP) ایمیل واقعی ارسال شود.

### افزوده‌شده
- **لاگ‌گذاری لینک بازیابی در حالت توسعه**: در محیط‌های `local`/`testing`/`development`، لینک بازیابی رمز عبور علاوه بر ایمیل در فایل `storage/logs/password-reset.log` نیز ثبت می‌شود تا بدون نیاز به SMTP قابل مشاهده باشد.
- **راهنمای فارسی در صفحه بازیابی**: متن صفحه `ForgotPassword` به فارسی ترجمه شد و در حالت توسعه پیامی مبنی بر مشاهده لاگ نمایش داده می‌شود.
- مستندات تنظیمات ایمیل در `.env.example` اضافه شد.

## [1.1.0] - 2026-08-23

### افزوده‌شده
- **یادداشت و برچسب برای بیس‌های نشان‌شده**: کاربران می‌توانند روی هر نقشهٔ نشان‌شده، یادداشت شخصی و برچسب‌های دلخواه (مثل `وار`، `فارم`، `TH16`) ثبت کنند.
- **تب «نشان‌شده‌ها» در داشبورد**: دسترسی سریع به بیس‌های ذخیره‌شده همراه با فیلتر بر اساس برچسب و صفحه‌بندی.
- **API مدیریت علاقه‌مندی‌ها**:
  - `POST /maps/{map}/favorite` — افزودن/حذف علاقه‌مندی همراه با notes/tags
  - `PUT /maps/{map}/favorite` — ویرایش یادداشت و برچسب‌های یک علاقه‌مندی موجود
  - `GET /maps/favorites?tag=...` — لیست علاقه‌مندی‌ها با فیلتر برچسب
- **مدل Pivot `MapFavorite`** برای cast خودکار فیلد JSON و مدیریت بهتر رابطهٔ چندبه‌چند.
- **Factory و تست‌های جدید** برای قابلیت علاقه‌مندی‌ها (`tests/Feature/MapFavoriteNotesTest.php`).

### اصلاح‌شده
- **باگ نرمال‌سازی تگ `DEMO`**: در `ClashOfClansService` حرف `O` در تگ آزمایشی `DEMO` اشتباهی به `0` تبدیل می‌شد و باعث برخورد به API واقعی می‌گردید. این رفتار اصلاح شد.

### یادداشت فنی
- تست‌های `StrategyLabControllerTest` و `StrategyLabVisionTest` در محیط فعلی به دلیل نبودن اکستنشن `fileinfo` PHP fail می‌شوند. این مشکل مربوط به محیط اجراست و ربطی به تغییرات کد ندارد.
